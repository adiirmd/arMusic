<?xml version="1.0" encoding="utf-8"?>
<!-- HTTPS only, everywhere, with no debug overrides shipped in release. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
